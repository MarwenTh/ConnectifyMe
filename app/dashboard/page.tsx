"use client";
import { SidebarMenu } from "@/components/Sidebar";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <SidebarMenu />
    </div>
  );
};

export default page;
